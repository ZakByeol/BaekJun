#include <cstdio>
#include <cstring>
// 아직 덜 품
bool GetTrue(char Input[]){

    return true;
}
int main(){
    char Input_value[10],Storage_value[10];
    int count[100];
    scanf("%s", Input_value);
    Storage_value[0] = Input_value[0];

    // 문자열을 돌려보면서, 이전 알파벳과 다르면, 해당 알파벳을 저장한 뒤, 카운트
    for (int i = 0; i < strlen(Input_value) - 1; ++i) {

    }

    return 0;
}